platform :ios, '9.0'

target 'TheFoodery' do
  use_modular_headers!

  # Pods for TheFoodery
    pod 'SDWebImage', :modular_headers => true
    pod 'Reusable', '~> 4.0.0' # is a Swift mixin for reusing views easily and in a type-safe way

end
